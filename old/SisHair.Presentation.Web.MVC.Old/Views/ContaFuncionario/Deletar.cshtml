@model SisHair.Presentation.Web.MVC.Old.Models.Conta.LoginFuncionario
@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/MasterAdiministrador.cshtml";
}

<div class="x_panel">
    <h3>Deseja realmente deletar esse registro?</h3>
    <div>        
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Usuario)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Usuario)
            </dd>
        </dl>
        <hr />
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <button class="btn btn-success" type="submit"><i class="fa fa-trash"></i> Deletar</button>
            <a href="@Url.Action("Index")" class="btn btn-outline-dark">
                <span class="fa fa-arrow-circle-left"></span> Lista de Usuários
            </a>
        </div>
        }
    </div>
</div>